#include <iostream>
#include <cstring>
#include <vector>

using namespace std;
class Token {
	public:
		Car(string _id, string _value, string _type) {
			string id = _id;
			string value = _value;
			string type = _type;
		}
};
int main()
{
	string line = "print 10";
	vector<string> line_split;
	for (i of )

	return 0;
}